/** Default expiration for HTTP request signatures (seconds). */
export const DEFAULT_EXPIRE_SECONDS = 300;

/** Maximum allowed clock skew for HTTP timestamp verification (seconds). */
export const HTTP_TIMESTAMP_TOLERANCE = 300;

/** Time-to-live for nonce deduplication cache (seconds). */
export const DEDUP_CACHE_TTL = 600;
